package orion.proto;

enum NodeType {
    NODE_PREMANENT = 0;
    NODE_TEMP = 1;
}

message DataValue {
    required string value = 1;
    required NodeType type = 2;
    optional string owner = 3;
    optional int64 prefix = 4;
}

